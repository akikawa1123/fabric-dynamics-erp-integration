# === 秘密情報（絶対にコミット禁止）===
.eventstream_connection.json
.env

# === 環境固有の設定（実値はコミット禁止。config.example.json のみ公開）===
config.local.json
config.json

# === 環境固有の生成物（スクリプト実行で再生成される）===
# Fabric のリソース ID / クラスタ URI 等のテナント固有情報を含むため除外
rti_info.json
dashboard_info.json
lakehouse_info.json
activator_info.json
warehouse_info.json
data_agent_info.json

# === Python ===
.venv/
__pycache__/
*.pyc

# === エディタ / OS ===
.vscode/
.DS_Store
Thumbs.db
